<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/LinearLayout1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@android:color/white"
    android:gravity="center_horizontal"
    android:padding="0dp">

    <!-- Ảnh nền nhạc -->
    <ImageView
        android:id="@+id/imageView1"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:contentDescription="@string/img_music_desc"
        android:scaleType="fitXY" />

    <!-- Khoảng cách trắng giữa ảnh và nút (nếu cần) -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="16dp" />

    <!-- Nút play -->
    <ImageButton
        android:id="@+id/imagstart"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_gravity="center"
        android:background="@android:color/transparent"
        android:contentDescription="@string/img_play_desc"
        android:scaleType="centerInside"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp" />

    <!-- Nút stop -->
    <ImageButton
        android:id="@+id/imgstop"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_gravity="center"
        android:background="@android:color/transparent"
        android:contentDescription="@string/img_stop_desc"
        android:scaleType="centerInside"
        android:layout_marginBottom="10dp" />

</LinearLayout>
